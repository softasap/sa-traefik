#!/bin/bash
echo "# This config is generated by startup script" > traefik_generated.toml

cat traefik.toml >> traefik_generated.toml

for file in `ls -v conf.d/*.toml`; do cat $file >>traefik_generated.toml; done
